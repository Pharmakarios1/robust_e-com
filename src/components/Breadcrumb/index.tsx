// import { Link } from "react-router";

// const Breadcrumb = ({ items }) => {
//   return (
//     <nav>
//       {items.map(({ item, index }: { item: string; index: number }) => (
//         <span>
//           {item.to ? (
//             <Link to={item.to}>{item.label}</Link>
//           ) : (
//             <span>{item.label}</span>
//           )}
//           {index < items.length - 1 && "/"}
//         </span>
//       ))}
//     </nav>
//   );
// };

// export default Breadcrumb;
